class TaskModel {
  final String title;
  final DateTime startDate;
  bool isCompleted;

  TaskModel({
    required this.title,
    required this.startDate,
    required this.isCompleted,
  });

  
}
